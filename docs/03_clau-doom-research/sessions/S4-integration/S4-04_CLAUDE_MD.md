# S4-04: CLAUDE.md 작성

> **세션**: S4 (문서 통합)
> **우선순위**: 🟡 medium
> **의존성**: S4-02, S4-03 완료
> **상태**: ⬜ 미시작 (이중 차단)

---

## 목적

Agent Teams 팀원이 spawn 시 자동 로드할 CLAUDE.md를 작성한다. 이 파일은 프로젝트의 핵심 컨텍스트를 담아, 새로운 Claude Code 세션이나 팀원이 별도 브리핑 없이 바로 작업을 시작할 수 있게 한다.

---

## CLAUDE.md에 포함할 내용

### 1. 프로젝트 개요 (5줄 이내)

- 프로젝트명, 한 줄 요약
- 핵심 설계 결정 3개
- 현재 단계 (연구 설계 / 실험 설계 / 실험 실행)

### 2. 핵심 규칙 (파괴 불가)

- 실시간 판단에 LLM 호출 없음 (RAG + Rust만)
- Python은 VizDoom 글루만
- 실험은 시드 고정 + 변수 격리 + A/B
- 에이전트당 동시 실험 최대 1개
- MD 파일이 에이전트의 DNA이자 프롬프트

### 3. 파일 경로 (필수 참조)

```
volumes/agents/active/DOOM_PLAYER_{SEQ}.MD  — 에이전트 정의
volumes/data/player-{SEQ}/game.duckdb       — 에이전트별 플레이 로그
volumes/research/RESEARCH_LOG.MD            — PI 연구 일지
volumes/research/orders/                    — 실험 지시서 (읽기 전용)
volumes/research/reports/                   — 실험 결과 리포트
```

### 4. DuckDB 스키마 요약

```sql
-- 실험 데이터
experiments: experiment_id, episode_id, variant, seed,
             survival_time, kills, damage_taken, ammo_efficiency,
             exploration_coverage, encounter_success_rate,
             ablation_condition, baseline_type

-- 교전 데이터
encounters: situation_snapshot, strategy_used, weapon_used,
            rag_query_result, outcome, duration_ms, decision_level

-- 세대 다양성
generation_diversity: generation_id, strategy_entropy,
                      behavioral_coverage, qd_score,
                      doc_pool_avg_distance, mutation_rate
```

### 5. 의사결정 계층

```
Level 0: MD 하드코딩 규칙 (Rust, < 1ms)
Level 1: DuckDB 로컬 캐시 (SQL, < 10ms)
Level 2: OpenSearch kNN (벡터 검색, < 100ms)
Level 3: Claude Code CLI (비동기, 수초) — 에피소드 간/세대 간에만
```

### 6. 기술 스택 요약

- 게임: VizDoom + Xvfb + noVNC (Docker)
- 에이전트: Rust / 글루: Python / 오케스트레이터: Go
- 검색: OpenSearch kNN / 저장: MongoDB + DuckDB
- 메시징: NATS pub/sub / 프로토콜: gRPC, WebSocket
- AI: Claude Code CLI (호스트) + Ollama (임베딩)

### 7. Agent Teams 파일 소유권

- 자기 담당 DOOM_PLAYER_{SEQ}.MD만 수정
- 자기 담당 DuckDB만 접근
- 연구 리포트는 Lead만 최종 작성
- 실험 지시서(orders/)는 읽기 전용

### 8. 현재 작업 (동적 주입)

```
[이 섹션은 세션/팀원 spawn 시 동적으로 주입]
예: "S1-01 진화/집단지능 문헌 수집 중. QD/MAP-Elites 카테고리 담당."
```

---

## 작성 원칙

- **간결함 우선**: 팀원이 10초 안에 컨텍스트를 파악할 수 있어야 한다
- **경로는 정확하게**: 오타 하나가 팀원의 작업을 방해한다
- **규칙은 명시적으로**: "하지 마라"를 명확히 (실시간 LLM 호출 금지 등)
- **동적 섹션 분리**: 고정 컨텍스트와 세션별 가변 컨텍스트를 분리

---

## 팀 구성 (Agent Teams)

| 역할 | 담당 범위 |
|------|----------|
| Lead | CLAUDE.md 최종 작성 및 검수 |
| Sub-agent A | 경로/규칙/스키마 정리 (S4-02 결과 참조) |
| Sub-agent B | 기술 스택/프로토콜 요약 (S4-03 결과 참조) |

---

## 완료 기준

- [ ] CLAUDE.md 초안 작성
- [ ] 모든 경로가 실제 프로젝트 구조와 일치하는지 검증
- [ ] DuckDB 스키마가 S2-02, S2-03의 확장 사항을 반영
- [ ] 테스트: 새 Claude Code 세션에서 CLAUDE.md만으로 작업 시작 가능한지 확인
- [ ] Lead 검수 완료
